[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200
remappings = []

[profile.default.fuzz]
runs = 512
seed = "0xdeadbeef"

[profile.default.invariant]
runs = 256
depth = 32
fail_on_revert = false
